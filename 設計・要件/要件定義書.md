# 要件定義書

**Ver.0.5**

作成日: 2026/07/10

最終更新日: 2026/09/18

## 1. 基本情報

| 項目 | 内容 |
| --- | --- |
| プロジェクト名 | HEW |
| システム | サイト上で描いた絵を、ポイントを使うオークションに出品・入札できるシステム |
| メンバー | 佐伯祐介　小村世羅　Naufal Abrisam Hilmy　瀧本隆人 |
| 本書の対象 | Ver0.5の機能・完成条件。Ver1以降の機能は対象外として区別する |

### 1.1 関連資料と位置付け

本書は関連設計と2026/09/18の仕様確認に基づく。仕様確認で合意した内容を優先し、既存の業務ロジック・画面設計の意図を要件に反映する。現行SQLの制約をそのまま製品要件とはせず、要件と一致しない箇所は10章の修正課題として扱う。

| 資料 | 参照する内容 |
| --- | --- |
| [ロジック・ファイル構造](ロジック-ファイル構造.md) | 作品作成、出品、入札・返却、認証、検索、お気に入りの処理 |
| [画面遷移図](画面遷移図.md)・[画面遷移メモ](画面遷移図-memo.md) | 画面構成と導線。将来機能を含むため、本書でVer0.5の範囲を区別する |
| [DB設計](db設計.md)・[ER図](ER図.md)・[deploy.sql](../DB-deploy/deploy.sql) | データ構造、関連、現行の制約 |
| [技術スタック](技術スタック.md)・[README](../readme.md) | 技術構成。Stripeの扱いは今回の合意で変更する |
| [メモ](memo.md) | 検討経緯。未確定の案や旧仕様を確定要件として扱わない |
| [関数定義](関数定義.md) | 現時点では空のため、今後の詳細設計で具体化する |

## 2. プロジェクト概要

ユーザーが制限時間内にサイト上で絵を描き、PNGの下書きとして保存した後、任意のタイミングで出品する。オークションは出品時に開始し、設定した終了日時まで開催する。描画の終了とオークションの終了は別の処理とする。

ユーザー登録・初投稿・描画などの操作に応じてボーナスポイントを付与し、そのポイントで入札する。Ver0.5ではStripe連携や実際の課金処理を行わない。

## 3. 基本的な利用の流れ

1. ユーザー名・メールアドレス・パスワードで登録し、メールアドレスとパスワードでログインする。
2. 描画時間を10秒・30秒・1分・5分から選んで描き始める。
3. 時間切れ、または途中の完成操作で描画を終了し、PNGを下書き保存する。
4. 作者が下書きから出品設定へ進み、タイトル・説明・タグ・開始価格・終了日時を設定して公開する。
5. 他のユーザーが商品詳細画面で入札する。手動入札に加え、上限額を指定した自動入札を利用できる。
6. 終了日時で入札受付を終了し、入札があれば最高入札者を落札者として確定する。入札がなければ落札不成立とする。

## 4. 対象ユーザー数・規模・利用環境

| 項目 | 内容 |
| --- | --- |
| 想定ユーザー数 | 100人 |
| 想定データ件数 | 作品100件、オークション履歴5,000件程度。件数の集計対象と整合性は9章で確認する |
| 対応端末 | PC・スマートフォン・タブレット |
| 推奨ブラウザ | Google Chrome・Safari |
| インターネット接続 | 必要 |

## 5. Ver0.5の機能要件

### 5.1 アカウント・認証

- 新規登録ではユーザー名・メールアドレス・パスワードを必須とする。ユーザー名とメールアドレスはそれぞれ一意とする。
- メールアドレスとパスワードでログインし、ログアウトできる。
- 表示名はユーザー名とは別に管理し、初期値を設定したうえで後から編集できる。
- 「メールアドレスでのログイン」は、確認メールによるアドレス所有確認とは区別する。所有確認の要否は未決事項とする。

### 5.2 描画・下書き保存

- Canvas上で絵を描き、色やペンを変更できる。
- 描画開始前に制限時間を10秒・30秒・1分・5分から選択できる。開始後は制限時間を変更できない。
- 時間切れで描画を停止し、PNGを下書き保存する。制限時間内でも完成操作で終了・保存できる。
- 保存後は絵への加筆を認めない。タイトルなどの出品情報は編集できる。
- 下書きは作者だけが閲覧でき、アカウント画面の下書き一覧から出品設定へ進める。
- PNG形式・サイズ・寸法を検証して保存する。画像サイズ上限はテスト後に決定する。

### 5.3 出品・公開

- 出品対象はサイト上で描いて保存した下書きとする。
- 出品設定ではタイトル・説明・既存タグ・開始価格・終了日時を設定する。開始価格は0以上とする。
- 作者が出品を確定すると作品を公開し、オークションを開始する。予約開始はVer0.5対象外とする。
- 1作品につきオークションは最大1件とし、Ver0.5では再出品を行わない。
- 最低落札価格は使わない。出品者が希望する最低額は開始価格で設定する。
- 取消機能はVer1で実装する。Ver0.5では出品や入札を取り消す操作を提供しない。

### 5.4 手動入札・ポイントの消費と返却

- 商品詳細画面内で金額を入力・確認・確定する。専用の入札画面は設けず、成功後は同じ画面で最新価格と結果を表示する。
- 入札は開催中かつ終了日時前に限る。出品者本人の入札は認めない。
- 入札額は現在価格より大きく、入札に必要なポイント残高を満たすことを条件とする。開始時の現在価格は開始価格とする。
- 入札受理時に入札額を残高から差し引く。高値更新時は前最高入札者に、その入札額を返却する。
- 同一オークションへの複数入札を履歴として保存する。
- 残高更新・入札履歴・ポイント取引履歴・現在価格を同一トランザクションで処理し、同時入札による不整合や残高のマイナスを防ぐ。

### 5.5 自動入札

- 入札者が対象オークションと希望する上限額を設定するユーザー機能とする。
- 他人が入札した際に、指定した上限額の範囲で上乗せして入札する。
- 手動入札と同様に、自己入札禁止・終了日時・残高確認・消費と返却のルールを適用する。
- 上限額が同額の場合は、設定日時が早いユーザーを優先する。
- 上乗せ幅などの詳細は9章の未決事項とする。
- 価格変動が起きているように見せる「吊り上げbot」とは別機能とし、botはVer0.5に含めない。

### 5.6 オークション終了・落札

- 設定した終了日時で入札受付を終了する。描画終了や作者の完成操作ではオークションを終了しない。
- 定期処理で終了対象を処理し、落札者・落札入札・確定額を記録する。確定処理の実行待ちでも終了日時を過ぎた入札は受理しない。
- 受理済みの入札があれば最高入札者が落札する。最低落札価格による不成立判定は行わない。
- 入札がなければ落札者なしで終了する。この場合、入札によるポイント消費はない。
- 落札額は入札時に差し引いているため、落札確定時に同額を再度差し引かない。
- 出品者へのポイント移転や落札後の作品の受け渡し方法は未決事項とする。

### 5.7 ボーナスポイント・チャージ画面

- ユーザー登録・初投稿・絵を描くなどの操作に応じて、ボーナスポイントを付与する画面を実装する。
- ボーナスポイントは実際の残高へ反映し、入札に使用できる。付与の履歴も保存する。
- 付与対象の詳しい成立条件・付与量・回数制限は未決事項とする。
- チャージ画面・課金／支払い画面は遷移先として用意する。Stripe API連携、テスト決済、実課金、決済によるポイント加算は実装しない。
- 現行の画面設計にある商品詳細からチャージ画面への導線はポイント不足時に限る。チャージ画面ではVer0.5で課金機能が未提供であると分かるようにする。

### 5.8 検索・お気に入り

- 公開作品をタグで検索できる。複数タグの指定はAND条件とする。
- 出品時は初期登録済みのタグから複数選択する。同じ作品への同一タグの重複付与は認めない。
- 作品のお気に入りとオークションのお気に入りを別々に登録・解除できる。
- アカウント画面では作品とオークションのお気に入り一覧を分けて表示する。

### 5.9 通知

- 入札・落札などの取引通知を、アプリ内と登録メールアドレスへのメールで提供する。
- アプリ内通知の一覧・既読状態を管理し、関連する商品詳細へ移動できる。
- メール通知の送信先・配信状態・送信日時を記録する。
- 通知種別ごとの送信対象・タイミング・再送条件は詳細設計で確定する。課金機能がないVer0.5では課金結果の通知は対象外とする。

## 6. 対象外機能・将来拡張

| 機能 | 扱い |
| --- | --- |
| 出品・入札の取消 | Ver1で実装。取消可能条件はVer1の設計時に決定する |
| 吊り上げbot | 価格変動が起きているように見せる機能。自動入札とは別の将来機能 |
| Stripeによるチャージ・課金処理 | 将来機能。Ver0.5は遷移先の画面のみ |
| 最低落札価格 | 将来拡張用のDB項目として残せるが、Ver0.5の画面・落札判定では使わない |
| ライブ配信・配信中のコメント・スーパーチャット | 将来機能 |
| 管理画面 | 商品・アカウント・タグの管理画面はVer0.5対象外。初期タグなどは開発用コマンドで準備する |
| タイムラプス・印刷・NFT | 将来機能 |
| 予約開始・再出品 | Ver0.5対象外 |

## 7. 非機能要件・権限・技術構成

### 7.1 操作性

- 描画、下書き保存、出品、入札の各操作を分かりやすい日本語で表示する。
- スマートフォンやタブレットでも主要操作を行いやすい配置とする。
- 描画の制限時間とオークションの終了日時を混同しないように表示する。

### 7.2 アクセス権限

| 利用者・立場 | 可能な操作 |
| --- | --- |
| ゲスト | 公開作品の閲覧・タグ検索 |
| ログインユーザー | 描画、自分の下書き閲覧・出品情報の編集・出品、他人のオークションへの入札・自動入札、お気に入り、ボーナスポイント機能、自分の通知・残高の確認 |
| 出品者 | 自分の下書きから出品する。自分のオークションへの入札は禁止。保存後の加筆やVer0.5での取消は不可 |
| 管理者 | DB上の権限は保持できるが、Ver0.5の管理画面は対象外 |

### 7.3 セキュリティ・整合性

- パスワードはハッシュ化して保存し、ログイン状態はセッションで管理する。
- 描画・出品・入札・お気に入り・下書き閲覧・ボーナスポイント画面・個人情報の参照にはログインを必須とする。
- 状態を変更するフォームにCSRF対策を適用する。下書き・通知などは所有者の権限を確認する。
- 入札処理で対象オークション・入札者・前最高入札者をロックし、最新の状態・金額・残高を確認する。
- 取引履歴を保護し、ユーザー・作品・オークションは原則として物理削除しない。
- 時刻はUTCで保存し、画面では日本時間で表示する。
- 秘密情報や接続情報は環境設定で管理し、ソースコードに直接記載しない。

### 7.4 技術構成

| 分野 | 構成 |
| --- | --- |
| サーバー | Python・Flask。Application FactoryとBlueprintで構成する |
| フロントエンド | HTML・CSS・JavaScript。描画にはCanvasを使用する |
| データベース | XAMPPのMariaDB。スキーマ変更はFlask-Migrateで管理する方針 |
| 認証・フォーム | Flask-Login、Werkzeugによるパスワードハッシュ、Flask-WTFによるCSRF対策 |
| 終了処理 | Flask CLIの終了処理をWindowsタスクスケジューラから定期実行する |

## 8. Ver0.5の完成条件

未決の数値・条件は9章で決定した後、以下の確認へ反映する。

| 対象 | 確認する結果 |
| --- | --- |
| 認証・権限 | 必須項目で登録し、メールアドレスでログイン・ログアウトできる。ゲストや他ユーザーが非公開の下書き・個人情報を操作できない |
| 描画 | 4種類の制限時間を選べ、開始後の変更を拒否する。時間切れ・途中完成のどちらでもPNGを保存し、保存後に加筆できない |
| 出品 | 下書きから出品情報を設定して公開でき、描画終了時点ではオークションを開始・終了しない |
| 入札 | 商品詳細内で複数のユーザーが競り上がれる。自己入札・不十分な入札額・残高不足・終了後の入札を拒否する |
| ポイント | ボーナスが残高と履歴へ反映され、入札時の消費・高値更新時の返却が整合する。同時操作でも残高を負にしない |
| 自動入札 | 他人の入札に応じて上限額・残高の範囲内で動作し、同額上限時は設定日時による優先順位を守る |
| 終了・落札 | 終了日時以降に入札できず、最高入札者・確定額が記録される。入札なしは落札者なしで終了し、ポイントを二重に差し引かない |
| 検索・お気に入り | タグAND検索と、作品・オークションそれぞれのお気に入り登録・解除・一覧表示ができる |
| 通知 | 決定した通知条件でアプリ内・メールの両方へ通知し、既読状態とメール配信結果を記録できる |
| 課金画面 | 遷移先を表示できるが、Stripe通信・決済・課金による残高変更は行わない |

## 9. 未決事項・今後の検証

| 項目 | 決定・検証する内容 |
| --- | --- |
| 課金上限額 | 未決。将来の課金機能で具体化する |
| 画像サイズ上限 | 保存・読み込み・端末負荷をテストしたうえで決定する |
| ボーナスポイント | 付与量、回数制限、登録・初投稿・描画などの成立条件、付与を実行するタイミングを決定する |
| 自動入札 | 上乗せ幅、設定直後の動作、上限額変更・停止、残高不足時の扱いを決定する |
| 最高入札者の再入札 | 残高確認と差額・返却の扱いを詳細設計で決定する |
| 通知 | 種別ごとの宛先・タイミング・メール送信失敗時の再送条件を決定する |
| 落札後の処理 | 出品者へのポイント移転、作品の閲覧・ダウンロードなどの受け渡し方法を決定する |
| 終了後の作品状態 | 入札なしで終了した作品の状態・公開表示を決定する。Ver0.5で再出品はしない |
| メールアドレスの所有確認 | 確認メールを使うかを決定する。メールとパスワードでのログイン自体は対象とする |
| 保存失敗時の動作 | 自動保存の失敗・通信断に対する再試行や画面表示を決定する |
| 規模・性能 | 作品100件とオークション履歴5,000件の集計対象を確認する。1作品1オークションの制約を踏まえ、負荷試験用の件数・同時利用数・応答時間の目標を決定する |
| Ver1の取消 | 取消可能条件と返却処理はVer1の設計時に決定する |

## 10. 関連設計・SQLの修正課題

以下は本書の要件に合わせて今後修正する箇所であり、現行設計や実装が対応済みであることを示すものではない。

| 関連資料・対象 | 現状と必要な調整 |
| --- | --- |
| deploy.sql・DB設計・ER図の入札件数 | `bids.auction_id`がUNIQUEのため、1オークションへの2件目の入札を保存できない。複数入札・自動入札のため制約と関連記述を変更する必要がある |
| DB設計の最低落札価格・終了理由 | 最低落札価格未満で不成立とする記述をVer0.5へ適用しない。`reserve_price`は未使用とし、描画終了を理由とするオークション終了も行わない |
| 画面遷移図・ロジックの下書き編集 | 描画画面へ戻って加筆する導線や「下書きの編集」の意味を修正する。保存後に編集できるのは出品情報とする |
| 描画時間の関連メモ | 3分固定や30秒だけの試験案ではなく、10秒・30秒・1分・5分から選択する仕様へ揃える |
| ポイント付与 | DBには`bonus`取引種別があるが、操作条件に応じた付与や回数制限を具体化する必要がある |
| README・決済／画面設計 | Stripeテストモードの利用記述をVer0.5へ適用しない。ボーナス付与機能と、遷移先だけのチャージ・課金画面を区別する |
| 自動入札・通知・関数定義 | DB項目だけでなく、Ver0.5で必要な処理・画面・確認項目を具体化する。関数定義は未記載 |
| 画面遷移図の配信・管理画面 | 将来機能の画面とVer0.5の対象画面を区別する |
| DBの取消状態・削除方針 | 状態項目が存在していてもVer0.5に取消操作を提供しない。取消条件はVer1で具体化する |

## 11. スケジュール

既存の計画を以下に引き継ぐ。実際の進捗に応じた日程変更は別途確認する。

| 時期 | 目標 |
| --- | --- |
| 夏休み | 画面設計・ER図・関数定義 |
| 9月 | Ver0.5の完成 |
| 10月 | テスト・Ver1の技術テスト・関数定義 |
| 11月 | Ver1の完成 |
| 12月 | テスト |
| 1月 | 予備 |

## 12. 変更履歴

| 日付 | 内容 |
| --- | --- |
| 2026/09/18 | 関連設計と仕様確認に基づきVer0.5の要件を再整理。描画時間選択・保存後の加筆禁止・出品と描画の分離・期限での落札・ボーナスポイント・自動入札とbotの区別・Stripe機能の対象外化・Ver1での取消を反映。完成条件、未決事項、関連設計の修正課題を追加 |
